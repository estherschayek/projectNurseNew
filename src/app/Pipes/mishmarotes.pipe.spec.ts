import { MishmarotesPipe } from './mishmarotes.pipe';

describe('MishmarotesPipe', () => {
  it('create an instance', () => {
    const pipe = new MishmarotesPipe();
    expect(pipe).toBeTruthy();
  });
});
