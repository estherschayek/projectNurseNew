import { MonthInStringPipe } from './month-in-string.pipe';

describe('MonthInStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthInStringPipe();
    expect(pipe).toBeTruthy();
  });
});
