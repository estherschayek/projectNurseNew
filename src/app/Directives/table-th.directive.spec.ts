import { TableTHDirective } from './table-th.directive';

describe('TableTHDirective', () => {
  it('should create an instance', () => {
    const directive = new TableTHDirective();
    expect(directive).toBeTruthy();
  });
});
