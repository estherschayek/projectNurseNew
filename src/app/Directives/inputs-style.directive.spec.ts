import { InputsStyleDirective } from './inputs-style.directive';

describe('InputsStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new InputsStyleDirective();
    expect(directive).toBeTruthy();
  });
});
