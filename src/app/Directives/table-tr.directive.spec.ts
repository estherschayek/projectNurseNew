import { TableTRDirective } from './table-tr.directive';

describe('TableTRDirective', () => {
  it('should create an instance', () => {
    const directive = new TableTRDirective();
    expect(directive).toBeTruthy();
  });
});
