import { ButtonsStyleDirective } from './buttons-style.directive';

describe('ButtonsStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonsStyleDirective();
    expect(directive).toBeTruthy();
  });
});
