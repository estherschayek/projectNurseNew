import { TablesDirective } from './tables.directive';

describe('TablesDirective', () => {
  it('should create an instance', () => {
    const directive = new TablesDirective();
    expect(directive).toBeTruthy();
  });
});
